{
  "name": "product-sales-management",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "start": "npm start --prefix frontend",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test",
    "eject": "cd frontend && npm run eject",
    "electron": "electron .",
    "electron-dev": "concurrently \"npm start\" \"wait-on http://localhost:3050 && electron .\"",
    "build:backend": "cd backend && pyinstaller backend.spec",
    "dist": "npm run build && npm run build:backend && electron-builder",
    "preelectron-pack": "npm run build",
    "backend": "cd backend && python app.py",
    "dev": "npm run electron-dev",
    "build-all": "npm run dist"
  },
  "dependencies": {
    "electron-updater": "^6.7.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^28.3.3",
    "electron-builder": "^24.9.1",
    "wait-on": "^7.2.0"
  },
  "build": {
    "appId": "com.burgerbhau.product-sales-management",
    "productName": "Product Sales Management",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "frontend/build/**/*",
      "electron/**/*",
      "node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "backend/dist/backend",
        "to": "backend"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "electron/assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "allowElevation": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Product Sales Management"
    }
  }
}